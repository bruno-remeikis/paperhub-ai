app = "seu-app-name"
primary_region = "gru"  # São Paulo

[build]
  dockerfile = "Dockerfile"

[http_service]
  internal_port = 8000
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0

[[vm]]
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1
```

## Estrutura do seu repositório
```
seu-projeto/
├── Dockerfile
├── fly.toml          ← Adicione este arquivo
├── .dockerignore
└── seu código...